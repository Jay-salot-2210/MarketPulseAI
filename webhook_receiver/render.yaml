services:
  - type: web
    name: marketpulse-webhook
    env: python
    region: ohio
    plan: free
    buildCommand: "pip install -r webhook_receiver/requirements.txt"
    startCommand: "uvicorn webhook_receiver.main:app --host 0.0.0.0 --port $PORT"